<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">
  <h3 class="mb-4 text-primary"><%= title %></h3>

  <p><strong>Môn học:</strong> <%= course.name %></p>
  <p><strong>Giới hạn:</strong> <%= course.maxStudents %> sinh viên</p>
  <p><strong>Đã đăng ký:</strong> <%= registrations.length %> sinh viên</p>

  <table class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>#</th>
        <th>Tên sinh viên</th>
        <!-- <th>Email</th> -->
        <th>Ngày đăng ký</th>
      </tr>
    </thead>
    <tbody>
      <% if (registrations.length === 0) { %>
        <tr><td colspan="4" class="text-center">Chưa có sinh viên đăng ký</td></tr>
      <% } else { %>
        <% registrations.forEach((reg, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= reg.userId.username || 'Chưa có tên' %></td>
            <!-- <td><%= reg.userId.email %></td> -->
            <td><%= new Date(reg.registeredAt).toLocaleString('vi-VN') %></td>
          </tr>
        <% }); %>
      <% } %>
    </tbody>
  </table>

  <a href="/admin/manage-courses" class="btn btn-secondary">Quay lại</a>
</body>
</html>
