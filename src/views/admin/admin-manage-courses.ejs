<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container mt-4">
      <header class="mb-4">
        <h1 class="text-center text-primary"><%= title %></h1>
        <nav class="navbar navbar-expand-lg navbar-light bg-light rounded">
          <div class="container-fluid">
            <a class="navbar-brand" href="/admin/dashboard">Admin Dashboard</a>

            <div class="d-flex">
              <a href="/create" class="btn btn-success">Thêm môn học mới</a>
            </div>

          </div>
        </nav>
      </header>

      <main>
        <h2><%= title %></h2>

        <form action="/search" method="GET" class="d-flex mb-4">
          <input type="text" name="q" placeholder="Tìm kiếm môn học..." />
          <button type="submit" class="btn btn-primary" style="margin-left: 5px;">Tìm kiếm</button>
        </form>

        <ul class="list-group">
          <% if (courses.length > 0) { %>
            <% courses.forEach(course => { %>
              <li class="list-group-item d-flex justify-content-between align-items-center">
                <div>
                  <span class="badge bg-primary">ID: <%= course.id %></span>
                  <a href="/course/<%= course.id %>" class="text-decoration-none text-dark"><%= course.name %></a>
                </div>

                <div>
                  <a href="/admin/courses/<%= course._id %>/registrations" class="btn btn-sm btn-outline-info">Xem DS sinh viên</a>
                  
                  <a href="/edit/<%= course.id %>" class="btn btn-warning btn-sm">Sửa</a>
                  <form action="/delete/<%= course.id %>" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc muốn xóa môn học này không?');">Xóa</button>
                  </form>
                </div>
              </li>
            <% }); %>
          <% } else { %>
            <li>Không có môn học nào.</li>
          <% } %>
        </ul>
      </main>

      <footer class="mt-4">
        <p class="text-center text-muted">Poghao &copy; 2025 - Quản lý môn học</p>
      </footer>
    </div>
  </body>
</html>

