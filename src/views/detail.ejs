<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container mt-4">
        <header class="mb-4">
            <h1 class="text-center text-info"><%= title %></h1>
        </header>

        <main>
            <div class="card p-4 shadow-sm">
                <p class="text-muted">Mã môn học: <%= course.id %></p>
                <p class="card-text">Số lượng sinh viên tối đa: <%= course.maxStudents%></p>

                <p class="card-text">
                    Thời gian mở đăng ký: <%= new Date(course.registrationStart).toLocaleString('vi-VN') %>
                </p>
                <p class="card-text">
                    Thời gian đóng đăng ký: <%= new Date(course.registrationEnd).toLocaleString('vi-VN') %>
                </p>

                <h2 class="card-title"><%= course.name %></h2>
                <p class="card-text"><%= course.description %></p>

                <% if (course.schedule && course.schedule.length > 0) { %>
                    <h5 class="mt-4">📅 Lịch học</h5>
                    <ul class="list-group list-group-flush">
                      <% course.schedule.forEach(item => { %>
                        <li class="list-group-item">
                          <strong><%= item.day %></strong>: từ <%= item.startTime %> đến <%= item.endTime %>
                        </li>
                      <% }) %>
                    </ul>
                <% } else { %>
                    <p class="text-muted mt-3">Chưa có lịch học.</p>
                <% } %>
            </div>
        </main>

        <footer class="mt-4">
            <p class="text-center text-muted">&copy; 2025 - Quản lý môn học</p>
        </footer>
    </div>
</body>
</html>
